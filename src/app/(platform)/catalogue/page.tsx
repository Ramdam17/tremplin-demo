import { PlaceholderState } from "@/components/ui/placeholder-state"
import { PageContainer } from "@/components/layout/page-container"

export default function CataloguePage() {
    return (
        <PageContainer>
            <PlaceholderState
                title="Catalogue Métiers"
                description="Explorez prochainement notre base de données de 1500+ métiers avec les données du marché en temps réel (Tension, Salaire, Offres)."
            />
        </PageContainer>
    )
}
